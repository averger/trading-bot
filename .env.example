# Exchange API Credentials
# WARNING: Never commit .env to git!
# Create API key with TRADE permission only — NO withdrawal!
# Enable IP whitelist on the exchange for extra security.

EXCHANGE_NAME=bybit
API_KEY=your_api_key_here
API_SECRET=your_api_secret_here

# Set to true for paper trading (ALWAYS start here)
TESTNET=true

# Trading pairs (comma-separated)
SYMBOLS=BTC/USDT,ETH/USDT

# Market type: "spot" or "futures" (futures required for shorts)
MARKET_TYPE=futures

# Timeframe
TIMEFRAME=1h

# Telegram Notifications (optional)
# 1. Create bot: message @BotFather on Telegram, /newbot
# 2. Get chat ID: message @userinfobot, or send /start to your bot
#    then visit https://api.telegram.org/bot<TOKEN>/getUpdates
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
